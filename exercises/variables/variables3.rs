// variables3.rs
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a hint.

// 선언 후 사용하기 위해선 let {변수명}:<Type>으로 선언해주자! 그냥 다 타입을 적어주는게 보기 편하긴해~
// int는 i32에용

fn main() {
    let x: i32 = 1;
    println!("Number {}", x);
}
